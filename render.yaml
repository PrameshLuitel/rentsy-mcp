services:
  - type: web
    name: rentsy-mcp
    env: python
    buildCommand: pip install -e . && cd frontend && npm install && npm run build
    startCommand: python src/app.py
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
